{% extends 'base.html.twig' %}

{% block title %}Suspect{% endblock %}

{% block body %}
    <h1>Suspect</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ suspect.id }}</td>
            </tr>
            <tr>
                <th>Nom Prenom</th>
                <td>{{ suspect.nom }}</td>
            </tr>
            
        </tbody>
    </table>

    <a  class="btn btn-info" href="{{ path('suspect_index') }}">Retour à la liste</a>

    <a  class="btn btn-warning"  href="{{ path('suspect_edit', {'id': suspect.id}) }}">editer {{suspect.nom}}</a>

    {{ include('suspect/_delete_form.html.twig') }}
{% endblock %}
