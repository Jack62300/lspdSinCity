<form method="post" action="{{ path('suspect_delete', {'id': suspect.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ suspect.id) }}">
    <button class="btn btn-danger">🚽 </button>
</form>
